{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Program",
      "skipFiles": [ "<node_internals>/**" ],
      "preLaunchTask": "tsc build",
      // idk what is up with the folder structure with tsc lol
      "program": "${workspaceFolder}/dist/UniversalControlMacros/src/app.js",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}